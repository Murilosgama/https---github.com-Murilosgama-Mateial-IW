import styles from './Footer.module.css';

function Footer(){
    return(
        <footer className ={styles.footer}>
            <h2>Desenvolvido por Murilo Gama &reg; 2024</h2>
        </footer>
    );
}

export default Footer;